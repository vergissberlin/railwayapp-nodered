[build]
builder = "RAILPACK"

[deploy]
startCommand = "node-red -u /data --settings /app/settings.js"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
