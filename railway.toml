[build]
builder = "RAILPACK"

[deploy]
startCommand = "node-red -u . --settings /app/settings.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
